{
  "name": "scibrain-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "SciBrain deployment package for Vercel (static frontend + serverless API)",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1"
  }
}

